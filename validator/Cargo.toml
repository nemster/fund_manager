[package]
name = "dummy_validator"
version = "1.0.0"
edition = "2021"

[dependencies]
scrypto = { version = "1.3.0" }

[dev-dependencies]
scrypto-test = { version = "1.3.0" }

[features]
default = []
test = []

[lib]
crate-type = ["cdylib", "lib"]

[workspace]
